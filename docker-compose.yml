version: '3.3'
services:
  ldes-server:
    env_file:
      - ./config.env
    container_name: ldes-server
    image: ldes-server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - ldes

networks:
  ldes:
    driver: bridge