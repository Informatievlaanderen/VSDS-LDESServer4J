name: Pull Request Quality Gate
on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  pull-request-quality-gate:
    uses: ./.github/workflows/templates/pull-request-quality-gate.yml@main
